<!DOCTYPE html>

<html>
    <head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
        <title><%= shortErrorMessage %></title>
        <style>
            :root {
                --img-size: min(100vw, 100vh);
            }
            .white {
                color: white;
            }
            body {
                background-color: black;
            }
            .default-font-family {
                font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            }
            p.centered {
                text-align: center;
                margin: 0 auto;
            }
            img.absolute-centered {
                display: block;
                margin: auto;
                text-align: center;
                position: absolute;
                top: 0;
                bottom: 0;
                left: 0;
                right: 0;
            }
            #error-code-container {
                container-type: inline-size;
            }
            #error-code-text {
                font-size: 10cqw;
                margin: 0.5em auto 0.5em auto;
            }
            #desperate-forkey {
                z-index: -1;
                width: var(--img-size);
                height: var(--img-size);
                opacity: 50%;
            }
            #long-error-message-text {
                max-width: var(--img-size);
            }
        </style>
    </head>
    <body class="black">
        <img class="absolute-centered" id="desperate-forkey" src="/desperate-forkey.jpeg">
        <div id="error-code-container">
            <p class="white centered default-font-family" id="error-code-text">Error Code <%= errorCode %></p>
        </div>
        <p id="long-error-message-text" class="white centered default-font-family"><%= longErrorMessage %></p>
    </body>
</html>